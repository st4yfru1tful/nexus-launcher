<Application x:Class="NexusLauncher.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
  <Application.Resources>
    <ResourceDictionary>
      <Color x:Key="NexusBackgroundColor">#10131C</Color>
      <Color x:Key="NexusPanelColor">#181D29</Color>
      <Color x:Key="NexusPanelRaisedColor">#202737</Color>
      <Color x:Key="NexusTextColor">#F7F9FC</Color>
      <Color x:Key="NexusMutedTextColor">#A4AEC2</Color>
      <Color x:Key="NexusAccentColor">#7B6CFF</Color>
      <Color x:Key="NexusAccentHoverColor">#958AFF</Color>
      <Color x:Key="NexusAccentTextColor">#A69CFF</Color>
      <Color x:Key="NexusFocusColor">#F7F9FC</Color>
      <Color x:Key="NexusInputColor">#111722</Color>
      <Color x:Key="NexusBorderColor">#354057</Color>
      <Color x:Key="NexusBorderStrongColor">#526583</Color>
      <Color x:Key="NexusHoverColor">#202A3C</Color>
      <Color x:Key="NexusSelectedColor">#30294E</Color>
      <Color x:Key="NexusOnAccentColor">#0B0E18</Color>
      <Color x:Key="NexusDangerSurfaceColor">#3A1C29</Color>
      <Color x:Key="NexusDangerBorderColor">#98566E</Color>
      <Color x:Key="NexusScrimColor">#DD0A0D14</Color>
      <Color x:Key="NexusSidebarColor">#141923</Color>
      <Color x:Key="NexusHeaderColor">#E0141923</Color>
      <SolidColorBrush x:Key="NexusBackground" Color="{StaticResource NexusBackgroundColor}" />
      <SolidColorBrush x:Key="NexusPanel" Color="{StaticResource NexusPanelColor}" />
      <SolidColorBrush x:Key="NexusPanelRaised" Color="{StaticResource NexusPanelRaisedColor}" />
      <SolidColorBrush x:Key="NexusText" Color="{StaticResource NexusTextColor}" />
      <SolidColorBrush x:Key="NexusMutedText" Color="{StaticResource NexusMutedTextColor}" />
      <SolidColorBrush x:Key="NexusAccent" Color="{StaticResource NexusAccentColor}" />
      <SolidColorBrush x:Key="NexusAccentHover" Color="{StaticResource NexusAccentHoverColor}" />
      <SolidColorBrush x:Key="NexusAccentText" Color="{StaticResource NexusAccentTextColor}" />
      <SolidColorBrush x:Key="NexusFocus" Color="{StaticResource NexusFocusColor}" />
      <SolidColorBrush x:Key="NexusInput" Color="{StaticResource NexusInputColor}" />
      <SolidColorBrush x:Key="NexusBorder" Color="{StaticResource NexusBorderColor}" />
      <SolidColorBrush x:Key="NexusBorderStrong" Color="{StaticResource NexusBorderStrongColor}" />
      <SolidColorBrush x:Key="NexusHover" Color="{StaticResource NexusHoverColor}" />
      <SolidColorBrush x:Key="NexusSelected" Color="{StaticResource NexusSelectedColor}" />
      <SolidColorBrush x:Key="NexusOnAccent" Color="{StaticResource NexusOnAccentColor}" />
      <SolidColorBrush x:Key="NexusDangerSurface" Color="{StaticResource NexusDangerSurfaceColor}" />
      <SolidColorBrush x:Key="NexusDangerBorder" Color="{StaticResource NexusDangerBorderColor}" />
      <SolidColorBrush x:Key="NexusScrim" Color="{StaticResource NexusScrimColor}" />
      <SolidColorBrush x:Key="NexusSidebar" Color="{StaticResource NexusSidebarColor}" />
      <SolidColorBrush x:Key="NexusHeader" Color="{StaticResource NexusHeaderColor}" />

      <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource NexusBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource NexusText}" />
        <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
        <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
      </Style>
      <Style TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource NexusText}" />
        <Setter Property="Background" Value="{DynamicResource NexusPanelRaised}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NexusBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="12,7" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="Button">
              <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7" Padding="{TemplateBinding Padding}">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                  <Setter TargetName="border" Property="Background" Value="{DynamicResource NexusAccentHover}" />
                  <Setter Property="Foreground" Value="{DynamicResource NexusOnAccent}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True"><Setter TargetName="border" Property="Opacity" Value="0.78" /></Trigger>
                <Trigger Property="IsEnabled" Value="False"><Setter TargetName="border" Property="Opacity" Value="0.43" /></Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource NexusFocus}" /><Setter TargetName="border" Property="BorderThickness" Value="2" /></Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{DynamicResource NexusAccent}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NexusAccent}" />
        <Setter Property="Foreground" Value="{DynamicResource NexusOnAccent}" />
      </Style>
      <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource NexusInput}" />
        <Setter Property="Foreground" Value="{DynamicResource NexusText}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NexusBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="CaretBrush" Value="{DynamicResource NexusText}" />
      </Style>
      <Style TargetType="ComboBoxItem">
        <Setter Property="Foreground" Value="{DynamicResource NexusText}" />
        <Setter Property="Padding" Value="11,8" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ComboBoxItem">
              <Border x:Name="surface" Background="Transparent" CornerRadius="7" Padding="{TemplateBinding Padding}">
                <ContentPresenter />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsHighlighted" Value="True">
                  <Setter TargetName="surface" Property="Background" Value="{DynamicResource NexusHover}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                  <Setter TargetName="surface" Property="Background" Value="{DynamicResource NexusSelected}" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource NexusPanel}" />
        <Setter Property="Foreground" Value="{DynamicResource NexusText}" />
        <Setter Property="BorderBrush" Value="{DynamicResource NexusBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="11,7" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="ComboBox">
              <Grid>
                <ToggleButton x:Name="toggleButton"
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}"
                              Focusable="False"
                              ClickMode="Press"
                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                  <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                      <Border x:Name="surface"
                              Background="{TemplateBinding Background}"
                              BorderBrush="{TemplateBinding BorderBrush}"
                              BorderThickness="{TemplateBinding BorderThickness}"
                              CornerRadius="10"
                              Padding="{TemplateBinding Padding}">
                        <Grid>
                          <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="22" />
                          </Grid.ColumnDefinitions>
                          <ContentPresenter Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                            ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                            ContentTemplateSelector="{Binding ItemTemplateSelector, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                                            VerticalAlignment="Center"
                                            TextElement.Foreground="{DynamicResource NexusText}" />
                          <Path Grid.Column="1"
                                Data="M 0 0 L 7 0 L 3.5 4 Z"
                                Fill="{DynamicResource NexusMutedText}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
                        </Grid>
                      </Border>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                          <Setter TargetName="surface" Property="BorderBrush" Value="{DynamicResource NexusAccent}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                          <Setter TargetName="surface" Property="BorderBrush" Value="{DynamicResource NexusAccent}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                          <Setter TargetName="surface" Property="BorderBrush" Value="{DynamicResource NexusFocus}" />
                          <Setter TargetName="surface" Property="BorderThickness" Value="2" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                          <Setter TargetName="surface" Property="Opacity" Value="0.45" />
                        </Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </ToggleButton.Template>
                </ToggleButton>
                <Popup x:Name="PART_Popup"
                       AllowsTransparency="True"
                       IsOpen="{TemplateBinding IsDropDownOpen}"
                       Placement="Bottom"
                       PopupAnimation="Slide">
                  <Border MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                          MaxHeight="{TemplateBinding MaxDropDownHeight}"
                          Background="{DynamicResource NexusPanelRaised}"
                          BorderBrush="{DynamicResource NexusBorderStrong}"
                          BorderThickness="1"
                          CornerRadius="10"
                          Padding="5"
                          Margin="0,5,0,0">
                    <ScrollViewer CanContentScroll="True" SnapsToDevicePixels="True">
                      <ItemsPresenter />
                    </ScrollViewer>
                  </Border>
                </Popup>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Foreground" Value="{DynamicResource NexusText}" />
      </Style>
      <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource NexusText}" />
      </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
